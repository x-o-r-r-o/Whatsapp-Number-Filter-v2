# Whatsapp-Number-Filter-v2
Cross‑platform CLI tool that filters phone numbers to find which are registered on WhatsApp by automating WhatsApp Web. Supports Chrome, Firefox, and Edge on Windows, macOS, and Linux with single and multi‑threaded modes, headless operation, profile reuse, and configurable input/output via YAML.
